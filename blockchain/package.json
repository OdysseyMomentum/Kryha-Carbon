{
  "name": "blockchain",
  "version": "1.0.0",
  "description": "Blockchain Kryha",
  "scripts": {
    "migrate": "truffle migrate",
    "start": "NODE_ENV=development npm run migrate && serve ./build/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@truffle/hdwallet-provider": "^1.2.0",
    "dotenv-flow": "^3.2.0",
    "ganache-cli": "^6.12.1",
    "serve": "^11.3.2",
    "truffle": "^5.1.53"
  },
  "standard": {
    "ignore": [
      "./build"
    ]
  }
}
